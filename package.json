{
  "name": "pomodoro_app",
  "version": "1.0.0",
  "description": "Study timer app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . PomodoroApp --platform=darwin --arch=x64 --icon=icon.icns --overwrite"
  },
  "author": "Pedro S Torres",
  "license": "ISC",
  "devDependencies": {
    "electron": "^36.2.1",
    "electron-packager": "^17.1.2"
  }
}
